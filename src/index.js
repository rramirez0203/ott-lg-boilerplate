import React from 'react';
import ReactDOM from 'react-dom';
import "./style/index.scss"

const title = 'LG BOILERPLATE';

ReactDOM.render(
  <div className="title">{title}</div>,
  document.getElementById('app')
);